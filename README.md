# dietai

Static site (`index.html`) with a Cloudflare Worker AI backend in `worker/`
that powers the AI food logger and daily AI tip. See `worker/README.md` to
deploy it and connect it via the `AI_ENDPOINT` constant in `index.html`.